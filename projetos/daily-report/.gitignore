node_modules/
dist/

# Variáveis de ambiente/segredos (a URL SAS do Power Automate embute o `sig`).
.env
.env.*
*.env
environment.env

# Extrações brutas do TASY e histórico contêm dados operacionais/PHI — nunca versionar.
data/sample_data/
data/history/
data/db/
data/out/
data/*.xlsx

# A referência de setores já vive como JSON em src/ref/ (sem PHI); o xlsx bruto fica de fora.
